// Домашнее задание по Булевой логике

const hasLicence = true;
const age = 24;
const isDrunk = false;

if (hasLicence && (age >= 18) && (!isDrunk)) {
    console.log('Может!');
} else {
    console.log('Не может!');
}